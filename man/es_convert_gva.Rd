% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/es_readers.R
\name{es_convert_gva}
\alias{es_convert_gva}
\title{Convert GVA Data to Long Time Series Format}
\usage{
es_convert_gva(dat, hmap, freq)
}
\arguments{
\item{dat}{A data frame containing raw GVA data. Must include columns
appropriate for the frequency (see Details). If \code{freq} is not supplied,
a \code{frequency} column must be present with a single unique value of either
\code{"quarterly"} or \code{"annual"}.}

\item{hmap}{Optional. A named list used as the hierarchy/disaggregation map.
If \code{NULL} or missing, the predefined \code{"hmap_gva"} map is loaded via
\code{es_load_predefined_data()}. Only override this for custom mappings.}

\item{freq}{Optional. A string specifying the data frequency: \code{"quarterly"}
or \code{"annual"}. If omitted, frequency is auto-detected from the
\code{frequency} column in \code{dat}.}
}
\value{
A long-format time series object,
containing columns such as \code{time}, \code{meta.release_tag}, \code{meta.release_order},
\code{meta.price_basis}, \code{meta.unit}, \code{meta.name}, \code{meta.disaggregation_group},
and \code{value.level}.
}
\description{
A unified entry point for converting Gross Value Added (GVA) data sourced
from eSankhyiki into a standardised long-format time series. Supports both
quarterly and annual input data. Frequency can be supplied explicitly or
detected automatically from a \code{frequency} column in the data.
}
\details{
\strong{Quarterly data} (\code{freq = "quarterly"}) requires columns:
\code{year}, \code{quarter}, \code{industry}, \code{current_price}, \code{constant_price}.
Optional columns: \code{base_year}, \code{unit}, \code{frequency}.

\strong{Annual data} (\code{freq = "annual"}) requires columns:
\code{base_year}, \code{year}, \code{revision}, \code{industry}, \code{subindustry},
\code{institutional_sector}, \code{current_price}, \code{constant_price}.
Optional column: \code{unit}.

In both cases, valid column names from the full expected set are:
\code{base_year}, \code{series}, \code{year}, \code{indicator}, \code{frequency}, \code{revision},
\code{industry}, \code{subindustry}, \code{institutional_sector}, \code{quarter},
\code{current_price}, \code{constant_price}, \code{unit}.
Any column outside this set will cause an error.

The output is a standardised long-format object.
}
