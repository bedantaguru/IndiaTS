% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_excel_chart.R
\name{viz_excel_chart_layered_axis}
\alias{viz_excel_chart_layered_axis}
\title{Layered-axis time-series line or bar chart written as a native Excel chart}
\usage{
viz_excel_chart_layered_axis(
  d,
  file,
  title = "Time Series",
  subtitle = "(y-o-y, per cent)",
  sheet = "Data",
  year_col = "year",
  period_col = "period",
  colors = NULL,
  ymin = NULL,
  ymax = NULL,
  digits = 1L,
  show_avg = TRUE,
  avg_label = "Last 2-year Average:",
  avg_window = 8L,
  add_footnote = TRUE,
  footnote_msg = "Note: * denotes incomplete data.",
  bullet_type = c("option1", "option2", "option3", "custom"),
  custom_bullet_hex = "29BF",
  enable_gridline = TRUE,
  single_layered = FALSE,
  single_layer_col = "time",
  if_single_series_disable_legend = TRUE,
  chart_type = c("line", "bar"),
  legend_type = c("type1", "type2"),
  legend_on_data_till = 1,
  add_points = FALSE
)
}
\arguments{
\item{d}{A data frame containing key axis columns (\code{year_col} and \code{period_col}
by default, or \code{single_layer_col} if \code{single_layered = TRUE}) and one numeric
column per series. Every column other than the key columns becomes a
series, in order, and its column name is used as the series name.}

\item{file}{Output path for the \code{.xlsx} file.}

\item{title, subtitle}{Chart title and (smaller, italic) subtitle.}

\item{sheet}{Worksheet name.}

\item{year_col, period_col}{Names of the outer (year) and inner (period) axis
columns. Looked up by name; the function errors if they are absent.}

\item{colors}{Optional character vector of series colours (\code{"4472C4"} or
\code{"#4472C4"}), recycled to the number of series. Defaults to a built-in
palette. Each series colour is reused in its line/bar, its end-point label
and its average-box bullet.}

\item{ymin, ymax}{Value-axis limits. If \code{NULL} they are inferred from the data
as rounded "nice" bounds.}

\item{digits}{Number of decimals. Values are rounded to \code{digits}, and both the
data cells and the end-point labels are formatted to show exactly that many
decimals (so e.g. \code{18} displays as \code{18.0}). Default \code{1}.}

\item{show_avg}{Logical; draw the rounded-rectangle average box. Default \code{TRUE}.}

\item{avg_label}{Heading shown inside the average box.}

\item{avg_window}{Number of trailing \emph{complete} periods averaged per series
(8 quarters = 2 years; use 4 for half-yearly, 12 for monthly). Default \code{8}.}

\item{add_footnote}{Logical; if \code{TRUE} \emph{and} at least one period is incomplete
(\code{*}), a footnote is added to the chart. If there is no \code{*} in the data the
footnote is never shown. Default \code{TRUE}.}

\item{footnote_msg}{Footnote text, used only when the footnote is shown.}

\item{bullet_type}{Average-box bullet glyph: \code{"option1"} (default, Wingdings
diamond U+F076), \code{"option2"} (circled bullet U+29BF), \code{"option3"} (filled
circle U+2022), or \code{"custom"} (use \code{custom_bullet_hex}).}

\item{custom_bullet_hex}{Unicode code point (hex, e.g. \code{"29BF"}, \code{"25CF"})
used only when \code{bullet_type = "custom"}. Default \code{"29BF"}.}

\item{enable_gridline}{Logical; draw the faint major gridlines. Default \code{TRUE}.}

\item{single_layered}{Logical; if \code{TRUE}, uses a single-level category axis
based on \code{single_layer_col} instead of the two-level year/period axis. Default \code{FALSE}.}

\item{single_layer_col}{Character; name of the single axis column used when
\code{single_layered = TRUE}. Default \code{"time"}.}

\item{if_single_series_disable_legend}{Logical; if \code{TRUE} and there is exactly
one series to plot, the chart legend is hidden. Default \code{TRUE}.}

\item{chart_type}{Character; the style of chart to render: \code{"line"} (default)
or \code{"bar"} (clustered column chart).}

\item{legend_type}{Character; styling style for end-point data labels:
\code{"type1"} (default, colored text matching series without border) or
\code{"type2"} (white rectangular box with colored border and black text).}

\item{legend_on_data_till}{Numeric/Integer; number of trailing observations
(from the right) on which to display data labels. Can be any number or \code{Inf};
automatically bounded between \code{0} and the length of the dataset. Default \code{1}.}

\item{add_points}{Logical; if \code{TRUE}, adds circular hollow data point markers
to line charts. Default \code{FALSE}.}
}
\value{
(Invisibly) the normalised path to the written file.
}
\description{
Writes an \code{.xlsx} containing a native (fully editable) Excel chart from a
tidy data frame. Supports standard line or column (bar) charts.
The chart has a two-level category axis (period inner tier, year outer tier)
and works with any period labels: quarters (\code{Q1}..\code{Q4}), half-years (\code{H1},
\code{H2}), months (\code{Jan}..\code{Dec}, \code{M1}..\code{M12}), etc. Alternatively, a single-layer
axis can be generated.
}
\details{
Incomplete periods are flagged by a leading \code{*} in the period label (e.g.
\code{"*Q1"}, \code{"*H2"}, \code{"*Apr"}); for line charts, their line segments are drawn
dotted. There may be one or several and they are detected automatically.
Missing values (\code{NA}) in series are cleanly skipped in the chart and left as
empty cells in the worksheet.

The chart is hand-built as Office Open XML and injected into an
\pkg{openxlsx2} workbook, because charting packages cannot emit multi-level
axes, per-point dotted segments, chart user-shapes (the average box and
footnote), or selective end-point data labels. The package is re-zipped with
\code{\link[utils:zip]{utils::zip()}} using standard local headers.

Requires the \pkg{openxlsx2} package and a system \code{zip} program on the
\code{PATH} for \code{\link[utils:zip]{utils::zip()}} (bundled with Rtools on Windows). LibreOffice
under-renders the dotted segments; Microsoft Excel renders them correctly.
}
\examples{
\dontrun{
d <- data.frame(
  year = c(rep("2024-25", 4), rep("2025-26", 4), "2026-27"),
  period = c("Q1","Q2","Q3","Q4","Q1","Q2","Q3","Q4","*Q1"),
  GVA  = c(6.2,7.1,8.4,7.9,6.6,5.8,7.2,8.1,6.8),
  GDP  = c(6.5,7.3,8.6,8.0,6.8,6.0,7.5,8.2,7.0),
  check.names = FALSE)

# Standard Line Chart with markers and Boxed Labels on last 3 points
viz_excel_chart_layered_axis(d, "chart_line.xlsx", title = "Real GVA & GDP",
                         subtitle = "(\%)", legend_type = "type2",
                         legend_on_data_till = 3, add_points = TRUE)
}

}
\keyword{internal}
